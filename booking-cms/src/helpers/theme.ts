// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00796b', // Set your custom primary color here
    },
    secondary: {
      main: '#white', // Set your custom secondary color
    },
  },
});

export default theme;
